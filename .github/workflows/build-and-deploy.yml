name: Build and Deploy

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: self-hosted

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Log in to GHCR
      run: echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Build Docker image
      run: |
        IMAGE_NAME=ghcr.io/${{ github.repository_owner }}/java-springboot-basic:latest
        docker build -t $IMAGE_NAME -f docker/Dockerfile .

    - name: Push image
      run: |
        IMAGE_NAME=ghcr.io/${{ github.repository_owner }}/java-springboot-basic:latest
        docker push $IMAGE_NAME

    - name: Deploy on OpenShift
      run: |
        oc project project-x
        oc set image deploy/my-app my-app=ghcr.io/${{ github.repository_owner }}/java-springboot-basic:latest
        oc rollout restart deploy/java-springboot-basic
